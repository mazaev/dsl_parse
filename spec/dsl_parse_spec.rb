require "spec_helper"

RSpec.describe DSLParse do
  it "has a version number" do
    expect(DSLParse::VERSION).not_to be nil
  end

  it "does something useful in model DSLParse" do
    expect(true).to eq(true)
  end
end
